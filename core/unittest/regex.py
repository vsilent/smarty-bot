#!/usr/bin/env python
# -*- coding: utf-8 -*-



def test_days():
    """docstring for test_days"""
    req = ' two days with "hey don\'t forget about pizza"'
    day = re.search('^\s?([a-zA-Z]+?)\s(days|day)', req2 )



def test_minutes():
    """docstring for test_minutes"""
    req = ' two days with "hey don\'t forget about pizza"'
    day = re.search('^\s?([a-zA-Z]+?)\s(days|day)', req2 )

